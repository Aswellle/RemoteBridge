# RemoteBridge 生产环境部署与使用指南

> **适用版本**：v1.3.11（桌面端） / v1.0.0（服务端 & Web）  
> **目标读者**：需要将 RemoteBridge 部署到公网服务器、通过域名访问 Web 端，并在远离家庭/办公网络时仍能长期稳定连接桌面端的用户。

---

## 一、架构概述

```
互联网
  │
  ▼
[云服务器]
  ├── Caddy（TLS 终止，80/443 端口）
  ├── Relay Server（中继，仅监听容器内网 3001）
  └── Web Client（Next.js，仅监听容器内网 3000）

  ▲
  │ 桌面端主动向外发起 WebSocket 长连接（无需在家庭/公司网络开放任何端口）
  │
[家庭/办公室 PC]
  └── RemoteBridge 桌面端（Electron，outbound-only）
```

**核心优势**：桌面端始终是**主动向外连接** Relay Server，家庭路由器/防火墙不需要任何端口映射，NAT 穿透由此天然解决。只要云服务器可达，人在世界任何地方都能通过 Web 端访问桌面上的文件。

> **替代方案**：桌面端内置**本地中继服务器**（基于嵌入式 Relay），无需单独部署云服务即可在局域网内使用；跨网访问时仍推荐云部署以获得最佳稳定性。详见第五节。

---

## 二、云服务器选型与准备

### 2.1 最低配置建议

| 项目 | 最低 | 推荐 |
|------|------|------|
| CPU | 1 核 | 2 核 |
| 内存 | 512 MB | 1 GB |
| 硬盘 | 10 GB SSD | 20 GB SSD |
| 带宽 | 1 Mbps | 5 Mbps+ |
| 系统 | Ubuntu 22.04 LTS | Ubuntu 24.04 LTS |

> 国内用户推荐轻量云服务器（腾讯云轻量/阿里云轻量/华为云）；海外用户推荐 Vultr、DigitalOcean、Hetzner。

### 2.2 安全组 / 防火墙规则

只需开放以下入站端口：

| 端口 | 协议 | 用途 |
|------|------|------|
| 80 | TCP | HTTP（Caddy 自动重定向到 HTTPS，Let's Encrypt 验证） |
| 443 | TCP | HTTPS / WSS（Caddy TLS 终止） |
| 22 | TCP | SSH 管理（按需限制来源 IP） |

**Relay Server（3001）和 Next.js（3000）绑定在 Docker 内网，不对外暴露。**

### 2.3 安装 Docker 和 Docker Compose

```bash
# Ubuntu 22.04/24.04
curl -fsSL https://get.docker.com | sh
sudo usermod -aG docker $USER   # 让当前用户免 sudo 使用 docker
newgrp docker                   # 立即生效（或重新登录）
docker compose version          # 确认 Compose v2
```

---

## 三、域名配置

### 3.1 购买域名并解析

1. 在任意域名注册商（阿里云万网、Namecheap、Cloudflare 等）购买域名，例如 `example.com`。
2. 在 DNS 管理面板添加 **A 记录**：

   | 主机记录 | 记录类型 | 记录值 |
   |----------|----------|--------|
   | `rb` | A | `<云服务器公网 IP>` |

   完整域名为 `rb.example.com`，根据喜好也可使用 `@`（裸域）或其他子域。

3. DNS 生效通常需要 5 分钟～1 小时，可用以下命令验证：

   ```bash
   nslookup rb.example.com
   # 应返回你的服务器 IP
   ```

> **如果使用 Cloudflare CDN**：先将代理模式设为「DNS only」（灰云），等 TLS 证书签好后再按需开启。

---

## 四、在服务器上部署

### 4.1 拉取代码

```bash
# 登录服务器（替换为实际 IP）
ssh user@<服务器IP>

# 克隆仓库
git clone https://github.com/Aswellle/RemoteBridge.git /opt/remotebridge
cd /opt/remotebridge
```

### 4.2 生成 JWT 密钥

```bash
# 生成两个独立的 48 字节随机密钥（必须各自独立，不得相同）
openssl rand -base64 48   # 第一个：用作 JWT_SECRET
openssl rand -base64 48   # 第二个：用作 JWT_REFRESH_SECRET
```

复制输出结果，稍后填入 `.env`。

### 4.3 创建 `.env` 配置文件

```bash
cp .env.example .env
nano .env   # 或 vim .env
```

将以下内容按实际情况填写：

```env
# ===== JWT 密钥（将 openssl 生成的值替换到这里）=====
JWT_SECRET=<第一个 openssl rand 输出>
JWT_REFRESH_SECRET=<第二个 openssl rand 输出>

# ===== 环境 =====
NODE_ENV=production

# ===== 域名（替换为你的实际子域名）=====
DOMAIN=rb.example.com

# ===== CORS：允许来自 Web 端域名的请求 =====
# 服务端使用 validateOrigin() 校验 Origin（不再反射任意 Origin）：
#   - 白名单（ALLOWED_ORIGINS）→ 反射 Origin + 允许凭据
#   - 任意 localhost:*（含 127.0.0.1）→ 放行（开发友好，默认含 localhost:3000 和 localhost:9666）
#   - 其它来源 → 拒绝（响应不带 ACAO，浏览器会拦截）
ALLOWED_ORIGINS=https://rb.example.com

# ===== Web 端访问 Relay 的地址（经 Caddy 代理，路径前缀 /api/v1 和 /ws）=====
NEXT_PUBLIC_API_URL=https://rb.example.com/api/v1
NEXT_PUBLIC_WS_URL=wss://rb.example.com/ws

# ===== 限流（可选，使用 RL_* 前缀变量覆盖默认值）=====
# 以下为集成测试/高并发场景预留；生产环境不传则使用安全默认值。
RL_AUTH_MAX=10
RL_PIN_MAX=5
RL_REGISTER_MAX=5
RL_WINDOW_MS=60000

> `NEXT_PUBLIC_API_URL` 和 `NEXT_PUBLIC_WS_URL` 会在构建 Web 镜像时**内联到客户端 JS bundle**，修改后必须重新构建镜像才能生效（`docker compose build web`）。

### 4.4 首次构建并启动

```bash
cd /opt/remotebridge

# 构建三个服务的 Docker 镜像（首次较慢，约 5～15 分钟）
docker compose build

# 后台启动全部服务
docker compose up -d

# 查看启动状态
docker compose ps

# 查看 Relay 日志（确认无报错）
docker compose logs -f server
```

### 4.5 验证部署

```bash
# 健康检查（应返回 {"status":"ok",...}）
curl https://rb.example.com/health

# 在浏览器访问
# https://rb.example.com  → 应显示 RemoteBridge 连接页面
```

首次访问时 Caddy 会自动向 Let's Encrypt 申请 TLS 证书（约 10～30 秒），浏览器地址栏出现绿色锁即为成功。

---

## 五、桌面端配置（让家中 PC 连接到公网 Relay）

### 5.1 安装桌面端

从 [GitHub Releases](https://github.com/Aswellle/RemoteBridge/releases) 下载对应系统的安装包：

| 系统 | 包格式 | 说明 |
|------|--------|------|
| Windows | `.exe` (NSIS 安装器) | 支持 x64 / arm64 |
| macOS | `.dmg` | 目前构建 Apple Silicon (arm64)；x64 需自行打包 |
| Linux | `AppImage` | 通用可执行文件 |

> 桌面端内置 **electron-updater**，安装后会在启动时自动检查 GitHub Releases 有新版本并提示更新。

### 5.2 首次启动引导

首次打开桌面端会弹出**欢迎引导弹窗**，自动启动内置的本地中继服务器（用于局域网内快速体验）。弹窗提供以下选项：

- **「去设置开启自动启动」**：跳转设置页，开启「随桌面端启动」本地中继。
- **「稍后」/「关闭」**：跳过，后续可在设置中手动配置。

### 5.3 配置连接公网 Relay

1. 打开 PC 上的 **RemoteBridge 桌面端**。
2. 进入「**设置**」→「**Relay 服务器地址**」，将默认的 `127.0.0.1:3002` 改为：

   | 字段 | 值 |
   |------|-----|
   | Relay WebSocket URL | `wss://rb.example.com/ws` |
   | Relay API URL | `https://rb.example.com/api/v1` |

> **WebSocket 连接参数**：桌面端连接 Relay 时自动在 URL 后追加 `?type=host`（Web 客户端为 `?type=client`）。Relay 要求该参数，否则握手返回 close `4001 Missing or invalid parameters`。用户在设置页只需填写裸 URL（如 `wss://rb.example.com/ws`），参数由客户端自动拼接。

3. 保存后桌面端会自动向公网 Relay 重新注册并建立长连接。

> **设置页其他关键分区**：
> - **本地中继服务器**：在此机器直接运行内置 Relay，无需另开终端。端口/自动启动可配；状态与上方地址**相互独立**（上方「已连接」可能是连到外部 Relay）。
> - **文件接收路径**：按文件类型（图片/视频/文档/压缩包/Markdown）自定义上传文件的保存目录。
> - **通用**：主题（亮色/暗色）、最小化到托盘。
> - **关于**：版本号、Electron/Node/系统信息。

### 5.4 让桌面端开机自动运行

**Windows**：
- 打开「**任务计划程序**」→「创建基本任务」→ 触发器选「登录时」→ 操作选启动 `RemoteBridge.exe`。
- 或在「**启动**」文件夹（`Win+R` 输入 `shell:startup`）中放置快捷方式。

桌面端启动后会自动最小化到系统托盘，后台常驻，无需用户干预。

---

## 六、日常使用流程

### 6.1 Web 端连接桌面端

1. 在**桌面端**打开 RemoteBridge，点击「**生成连接码**」，得到 8 位 PIN 码（默认有效期 **5 分钟**，可在服务端配置上限至 24 小时）。
2. 在手机/平板/其他电脑浏览器访问 `https://rb.example.com`。
3. 在连接页面输入 PIN 码和本设备名称，点击「**连接**」。
4. 首次连接成功后，设备记录会保留在页面左侧历史面板，下次仍需生成新 PIN 重新认证（PIN 为一次性使用）。

### 6.2 文件操作（Web 端）

| 功能 | 操作 |
|------|------|
| 浏览文件 | 「文件浏览」页面 → 点击共享目录进入 |
| 预览图片/文档/视频 | 点击文件名即触发预览（PDF 在 sandboxed iframe 中打开） |
| 下载文件 | 预览界面内点击下载，或通过下载面板断点续传 |
| 发送文件（Web → 桌面） | 预览/目录界面选择上传，分块经 Relay 中转 |

### 6.3 消息通信

- **Web 端**：「消息」页面选择会话后实时收发文本。
- **桌面端**：「消息中心」页面与远程客户端双向通信；新消息到来时桌面通知提醒。

### 6.4 会话管理（桌面端）

- **信任设备**：在「已连接客户端」页面点击「信任」，标记常用设备。
- **吊销会话**：设备丢失或换手时，点击「吊销」立即断开该设备的 WebSocket 连接，旧 token 即刻失效。
- **查看活动日志**：「已连接客户端」页面的「活动日志」子标签可查看每次文件访问/下载记录。
- **安全审计日志**：「安全审计」独立页面记录认证失败、路径被阻止等安全事件。

---

## 七、保持长期稳定连接

### 7.1 桌面端自动重连机制

桌面端内置指数退避重连（1s → 2s → 4s → … → 最大 30s，**无限重试**）：

- 网络抖动、Relay 重启、PC 休眠唤醒后均会自动恢复连接，**无需任何手动操作**。
- 托盘图标：**绿色** = 已连接，**灰色** = 断开/正在重连。

### 7.2 防止 PC 休眠切断连接

长时间无人操作的 PC 若进入深度睡眠，网卡会断电，WebSocket 长连接随之中断（唤醒后会自动重连，但中间有空窗期）：

**Windows 设置防止睡眠**：
```
控制面板 → 电源选项 → 更改计划设置
  → "使计算机进入睡眠状态"：选「从不」
  → 显示器关闭时间：按需设置（不影响网络）
```

或使用命令行：
```powershell
# 永不休眠（仅屏幕保护）
powercfg /change standby-timeout-ac 0
powercfg /change standby-timeout-dc 0
```

### 7.3 Relay 服务器保活

Docker Compose 的 `restart: unless-stopped` 确保：

- Docker daemon 启动时自动拉起所有容器（等同开机自启）；
- 容器崩溃后 Docker 自动重启；
- 重启后两端自动重连，在途文件传输通过 HTTP Range 断点续传恢复。

在服务器上执行以下命令确认 Docker 开机自启：

```bash
sudo systemctl enable docker

```

### 7.4 数据保留策略

服务端内置 **90 天数据保留清理任务**（每天运行一次），自动清理超期的 `messages` 与 `security_logs` 表记录，防止 SQLite 数据库无限增长。清理以 **500 行/批** 进行，避免单次大量 DELETE 阻塞事件循环。清理日志可在 `docker compose logs server` 中确认。

`/health` 端点会返回数据库文件大小与各表行数，便于监控增长趋势。

### 7.5 二进制文件隧道

Host 的文件服务器只监听 `127.0.0.1`，Relay 跨 NAT 无法 HTTP 直连——文件内容借道 Host 已建立的**出站 WebSocket** 连接回传：

- Relay 发出 `CMD_FETCH_FILE`，Host 将文件切为 **256 KB** 分块，以**自描述二进制 WS 帧**（`file-tunnel-codec`，`encodeFileChunkFrame`）发送，避免 JSON+base64 编解码的额外内存拷贝。
- Relay 端用 `reply.hijack()` 流式写入 HTTP 响应，并手动补 CORS 头（`corsHeadersFor`），支持 `Range` 断点续传。
- 浏览器中断下载时 Host 侧因 WS 缓冲水位（4 MB 发送水位）自然停止，不会浪费带宽。

> 因此文件下载速度受两段网络限制：桌面端上行带宽 + Relay 服务器带宽（详见 FAQ Q4）。

### 7.6 外部健康监控（可选但推荐）

注册免费的 [Uptime Kuma](https://github.com/louislam/uptime-kuma)（可自建）或 [Healthchecks.io](https://healthchecks.io) 等服务，监控以下端点：

```
https://rb.example.com/health
```

当接口返回非 200 或 `"status":"error"` 时发送邮件/微信/Telegram 告警，第一时间感知 Relay 异常。

### 7.7 Host JWT 自动轮换

桌面端内置 **Host JWT 轮换器**（`token-rotator`），解决长期开机的桌面端 token 过期问题：

- **每日检查**：启动 30 秒后首次检查（等连接稳定），之后每 24 小时检查一次。
- **轮换阈值**：当 Host JWT 剩余有效期 **≤ 30 天**（`HOST_TOKEN_ROTATION_THRESHOLD_DAYS`）时，自动调用 `POST /auth/host-token-refresh` 换取新 token 并持久化到本地配置。
- **静默透明**：全程无需用户干预；渲染层设置页可查询 token 剩余天数（`host:get-token-expiry-days` IPC）。

> `POST /auth/host-token-refresh` 仅校验 JWT 签名（不查询 DB），返回新 token 与 `expiresAt`；被封禁的主机（`isBanned`）会被拒绝。轮换后旧 token 在其自然到期前仍有效。

---


## 八、日常运维速查

### 查看服务状态


```bash
cd /opt/remotebridge
docker compose ps                  # 各服务运行状态
docker compose logs -f server      # Relay 实时日志
docker compose logs -f caddy       # Caddy 日志（证书/路由问题看这里）
```

### 重启服务

```bash
docker compose restart server      # 仅重启 Relay
docker compose restart             # 重启全部服务（含 Web 和 Caddy）
```

### 更新到新版本

```bash
cd /opt/remotebridge
git pull origin main               # 拉取最新代码
docker compose build               # 重新构建镜像（修改了 .env 中的 URL 也需此步）
docker compose up -d               # 滚动更新
docker compose ps                  # 确认健康
```

### 查看数据库大小与行数

```bash
curl -s https://rb.example.com/health | python3 -m json.tool
# 输出示例：
# {
#   "status": "ok",
#   "version": "1.0.0",
#   "db": {
#     "sizeBytes": 1048576,
#     "tables": { "hosts": 3, "sessions": 28, "messages": 1540, "securityLogs": 220 }
#   }
# }
```

### 数据备份

SQLite 数据库存放于 Docker volume `server-data`（完整名取决于 Compose 项目名，通常为 `<项目名>_server-data`），定期备份：

```bash
# 将数据库文件复制到宿主机
docker run --rm \
  -v remotebridge_server-data:/data \
  -v $(pwd)/backup:/backup \
  alpine cp /data/remotebridge.db /backup/remotebridge-$(date +%Y%m%d).db
```

> 建议每周自动备份到对象存储（OSS/S3）。备份前可通过 `/health` 确认数据库大小。

---

## 九、常见问题排查

### Q1：浏览器访问域名显示「证书错误」

- 等待 30 秒后刷新，首次申请 Let's Encrypt 证书需要时间。
- 检查 80 端口是否开放：`docker compose logs caddy` 查找 `ACME` 或 `certificate` 关键字。
- 确认 DNS A 记录已生效：`nslookup rb.example.com` 应返回服务器 IP。

### Q2：桌面端托盘图标持续灰色，无法连接

1. 检查 Relay 是否正常：`curl https://rb.example.com/health`，应返回 `{"status":"ok"}`。
2. 检查桌面端设置里的 Relay 地址是否与部署域名一致（`wss://rb.example.com/ws`）。
3. 检查服务器防火墙是否放行了 443 端口（WSS 走 443，不是 3002）。
4. 若公司/校园网封锁了 WebSocket：使用手机热点测试；或联系网络管理员开放 443 WSS 流量。

### Q3：Web 端点击「连接」后提示「连接超时」或无响应

- 确认 PIN 码在有效期内使用（默认 5 分钟，过期需在桌面端重新生成）。
- 确认 Web 端的 `NEXT_PUBLIC_WS_URL` 与 Relay 域名一致（修改后需重新 `docker compose build web && docker compose up -d`）。
- 在浏览器开发者工具 Network 标签查看 WebSocket 握手是否成功（状态码应为 101）。

### Q4：文件下载速度慢

文件通过 Relay Server 中转（WS 文件隧道），速度受两段网络限制：

- **桌面端上行带宽**（家庭宽带通常为 10～50 Mbps 上行）。
- **Relay 服务器带宽**（轻量云服务器通常 1～5 Mbps）。

可升级云服务器套餐带宽，或选择国内 CDN 加速（需要额外配置）。

### Q5：长时间不用后 Web 端提示「会话已过期」

Access Token 有效期为 2 小时，Refresh Token 有效期为 30 天。Web 端会自动用 Refresh Token 续期，通常透明无感。若真正过期（30 天未使用）：

- 回到首页，在历史设备卡片提示下，让桌面端重新生成 PIN，完成重新认证即可。

---

## 十、安全建议

| 建议 | 说明 |
|------|------|
| Token 存储安全 | Web 端 Access/Refresh Token 仅通过 **httpOnly + Secure + SameSite=Strict** cookie 下发（`rb_access` 2h / `rb_refresh` 30d），响应体不再回传明文 JWT，XSS 脚本无法通过 `document.cookie` 读取 |
| 定期轮换 Host JWT | 桌面端每日在剩余 ≤ 30 天时自动轮换 Host token（详见 7.7），长期离线的旧 token 自然到期失效 |
| 不定期更换 JWT 密钥 | 更换 `.env` 中的 `JWT_SECRET`/`JWT_REFRESH_SECRET` 后重启 server，所有现存 token 立即失效，用户需重新连接 |
| 定期检查已连接客户端 | 桌面端「已连接客户端」页面定期核查，对陌生设备立即吊销 |
| 启用安全审计日志告警 | 频繁出现「认证失败」或「路径访问被阻止」时需警惕暴力破解；审计日志可在 Web 端「安全审计」页面查看 |
| 限制共享目录权限 | 只共享必要目录；系统目录（`C:\Windows`、`/etc` 等）已被自动拦截，无法加入白名单 |
| 服务器定期更新系统包 | `apt update && apt upgrade -y`，防止底层漏洞被利用 |
| 备份 SQLite 数据库 | 见第八节，建议每周自动备份到对象存储（OSS/S3） |
| 保护服务器 SSH | 禁用密码登录、使用密钥认证、修改默认端口、配置 fail2ban |

> **补充：httpOnly cookie 认证流程**（02a-S11）  
> - Web 端登录后，服务端通过 `Set-Cookie` 下发两个独立 cookie：`rb_access`（Access Token，2 小时）与 `rb_refresh`（Refresh Token，30 天），均带 `HttpOnly; Secure; SameSite=Strict` 属性。  
> - 后续 REST 请求通过 `extractTokenFromRequest` 提取 token：优先 Authorization 头，兜底 `rb_access` cookie；`/auth/refresh` 优先读 `rb_refresh` cookie，并在 cookie 路径的响应体中回传空串 `accessToken: ""` 以防 XSS 读取（SEC-C1）。  
> - WebSocket 连接前先调 `GET /auth/ws-ticket` 用 cookie 换取 30 秒单次票据（`?ticket=`），再建立 WS；Host 桌面端继续沿用 `?token=` 路径（Authorization 头）。

