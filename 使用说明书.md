# RemoteBridge 使用说明书

> 远程文件桥接系统 —— 服务启动、桌面端运行与全功能操作指南
> 适用版本：v1.3.11（2026-08-20 更新）

---

## 一、系统组成

RemoteBridge 由三部分组成,按以下关系协作:

```
┌──────────────┐   出站 WebSocket   ┌──────────────┐   WebSocket   ┌──────────────┐
│  桌面端 Host  │ ─────────────────→ │  Relay 中继   │ ←──────────── │  Web 客户端   │
│ (Electron)   │                    │  服务器       │               │  (浏览器)     │
│ 你的电脑      │                    │  :3002       │               │  :9666       │
└──────────────┘                    └──────────────┘               └──────────────┘
```

- **Relay 中继服务器**:消息转发枢纽,不存储任何文件。默认端口 `3002`。
- **桌面端(Host)**:安装在被访问电脑上的 Electron 应用,管理共享目录、生成连接码、审计访问。**永不对外开放端口**,只主动连接 Relay。
- **Web 客户端**:浏览器访问,凭连接码(PIN)连接到桌面端,远程浏览/预览/下载文件、收发消息。默认端口 `9666`。

**启动顺序建议:Relay → 桌面端 → Web**(顺序错了也能自愈,桌面端与 Web 都有无限重连机制)。

---

## 二、环境要求

| 依赖 | 版本 | 说明 |
|---|---|---|
| Node.js | 22.x | 项目实测于 v22.14.0 |
| pnpm | 9.x | `npm i -g pnpm` 或 corepack |
| 操作系统 | Windows / macOS / Linux | 本文以 Windows 为例 |

---

## 三、首次安装

在项目根目录(`D:\AI\remotebridge`)执行:

```sh
# 1. 安装全部依赖
pnpm install

# 2. 构建共享协议包(必须先于一切应用构建/运行)
pnpm --filter @remotebridge/shared build
```

> ⚠️ **better-sqlite3 注意事项**:服务器(Node)与桌面端(Electron)需要不同的原生二进制。
> 仓库已内置自动切换机制(`.cache/better_sqlite3.electron.node` + dlopen 钩子)。
> 若 Relay 启动时报 `NODE_MODULE_VERSION` 不匹配错误,执行:
> ```sh
> pnpm --filter @remotebridge/server rebuild better-sqlite3
> ```

---

## 四、启动服务

### 4.1 启动 Relay 中继服务器

**首次使用前**：确认 `apps/server/.env` 中的 `JWT_SECRET` 与 `JWT_REFRESH_SECRET` 已配置为两个独立的强密钥（各执行一次 `openssl rand -base64 48` 填入）。`NODE_ENV=production` 时服务器启动会自动校验，弱密钥或默认占位符将拒绝启动。

#### 生产模式（日常推荐）

```sh
# 步骤 1：构建（shared 与 server 均需编译，Git Bash 中运行）
bash scripts/deploy-server.sh

# 步骤 2：启动（读取 apps/server/.env，确保 NODE_ENV=production）
cd apps/server && node dist/index.js
```

#### 开发模式（热重载，调试用）

```sh
pnpm --filter @remotebridge/server dev
```

看到以下输出即成功：

```
🚀 RemoteBridge Relay Server 启动于 http://0.0.0.0:3002
📡 WebSocket 端点: ws://0.0.0.0:3002/ws
🔗 健康检查: http://0.0.0.0:3002/health
```

验证：`curl http://127.0.0.1:3002/health` 返回 `{"status":"ok",...}`。

主要环境变量（配置于 `apps/server/.env`）：

```
RELAY_PORT=3002                      # 监听端口
JWT_SECRET=<openssl rand -base64 48> # access token 签名密钥
JWT_REFRESH_SECRET=<另一独立随机串>    # refresh token 签名密钥
ALLOWED_ORIGINS=https://your-domain.com   # CORS 来源白名单，逗号分隔多个
NODE_ENV=production                  # 生产模式，启用密钥强度校验
RB_DATA_DIR=                         # 数据库目录，留空使用 ~/.remotebridge/data
```

> **生产部署安全要求**(详见 `.env.example` 与 `docker-compose.yml`):
> - `ALLOWED_ORIGINS` 必须显式设置为 Web 客户端的真实来源(协议+域名,逗号分隔多个)。
>   未设置时回退为 `http://localhost:9666`,会导致部署后的 Web 客户端被 CORS 拦截。
> - Relay 务必部署在 TLS 之后(参见 `docker-compose.yml` 中的 Caddy 反向代理,自动签发
>   Let's Encrypt 证书)。PIN 码、JWT access/refresh token、`register-host` 返回的 Host
>   secret 均经此连接传输,明文 HTTP 会暴露这些凭证。

### 4.2 启动 Web 客户端

```sh
pnpm --filter @remotebridge/web dev
```

启动后浏览器访问 **http://localhost:9666**。

生产模式:`pnpm --filter @remotebridge/web build && pnpm --filter @remotebridge/web start`

### 4.3 构建并打开桌面端程序

**方式 A:构建后直接运行(推荐,日常使用)**

```sh
# 构建(产物在 apps/desktop/dist/)
pnpm --filter @remotebridge/desktop build

# 启动(Windows,在项目根目录执行)
cd apps/desktop
.\node_modules\.bin\electron .
```

**方式 B:开发模式(带热重载,改代码时用)**

```sh
pnpm --filter @remotebridge/desktop dev
```

> 若开发模式因 better-sqlite3 报错,在 `apps/desktop` 下执行 `npx @electron/rebuild -f -w better-sqlite3` 手动 rebuild,
> 之后记得为服务器执行 `pnpm rebuild better-sqlite3` 恢复。

**方式 C:打包为安装程序(分发给最终用户)**

```sh
pnpm --filter @remotebridge/desktop package:win    # Windows(另有 :mac / :linux)
```

产物为 electron-builder 安装包,安装后从开始菜单启动。

**一键启动全部(开发用)**:根目录执行 `pnpm dev`(turbo 并行启动三端)。

---

## 五、桌面端操作流程

### 5.1 首次启动与自动连接

桌面端启动后会**自动注册并连接 Relay**,窗口左下角显示"已连接"即就绪。主机身份(hostId)持久化保存,重启后复用,不会重复注册。

> ⚠️ 桌面端默认连接 `ws://127.0.0.1:3002/ws`。**不要使用 `localhost`** —— 它会被解析为
> IPv6 `::1` 而 Relay 只监听 IPv4,导致连接失败。如需连接远程 Relay,在「设置」页
> 填写实际 IP 或域名。

配置文件位置(手工排查时使用):
`C:\Users\<用户名>\AppData\Roaming\@remotebridge\desktop\remotebridge-config.json`

### 5.2 添加共享目录(必做)

Web 端只能访问白名单中的目录:

1. 左侧导航进入 **「共享目录」**;
2. 点击添加,选择要共享的文件夹;
3. 为目录设置权限:
   - **只读**:Web 端可浏览、预览;
   - **可下载**:在只读基础上允许下载文件;
4. 可为目录设置别名(Web 端显示的名称)。

> 系统关键目录(如 `C:\Windows`、`C:\Program Files`)被内置黑名单拦截,无法共享。

### 5.3 生成连接码(PIN)

1. 进入 **「连接状态」** 页;
2. 点击 **「生成连接码」**,得到 8 位 PIN(如 `7RKY-N5G2`);
3. 把 PIN 告知要连接的人(或自己在浏览器输入)。

PIN 规则:
- **一次性使用**,连接成功即失效;
- 默认 **5 分钟过期**(可在生成时选择时长);
- 字符集排除易混淆字符(无 0/O/I/1/l);
- 每分钟最多生成 5 次。

### 5.4 已连接客户端管理(信任 / 吊销)

进入 **「已连接客户端」** 页,列表实时显示每个客户端的在线状态:

- **信任 / 取消信任**:为设备打信任标记(本地记录,可随时切换);
- **吊销**:弹出确认后立即作废该会话 —— 对方被**强制断开**并踢回连接页,必须重新获取 PIN 才能连接;
- **活动日志**标签页:查看每个客户端的文件访问记录(含被拦截的越权请求)。

### 5.5 消息中心 / 安全审计 / 设置

- **消息中心**:与已连接的 Web 客户端互发文字消息,并接收从 Web 端发来的文件;
- **安全审计**:查看全部安全事件(连接、吊销、路径拦截等);
- **设置**(全部**保存即生效**,无需重启):
  - Relay 服务器地址(WebSocket + API):变更后自动断开并按新地址重连,结果即时反馈;
  - 开机自启 / 最小化到托盘;
  - 主题(暗色 / 亮色):保存后立即切换,亮色模式已采用全新配色方案(高对比度冰蓝色系)。

### 5.6 自动更新

打包安装版（非开发模式）启动约 10 秒后会静默检查 GitHub Releases 是否有新版本，窗口顶部显示更新横幅：

| 横幅状态 | 含义 | 操作 |
|---|---|---|
| 发现新版本 vX.X.X | 可用更新就绪 | 点击**下载**开始后台下载 |
| 下载中 X% | 正在拉取安装包 | 等待完成即可 |
| 已下载，可安装 | 安装包已就绪 | 点击**重启并安装**，程序重启后自动完成升级 |
| 检查失败 / 网络错误 | 简短的友好错误说明 | 点击横幅右侧 **×** 关闭，不影响正常使用 |

> 更新包从 GitHub Releases 拉取，无法访问 GitHub 时忽略横幅即可——程序不会强制更新，也不会重复弹出（关闭后本次运行不再提示）。

---

## 六、Web 端操作流程

### 6.1 连接到远程电脑

1. 浏览器打开 **http://localhost:9666**;
2. 输入桌面端生成的 8 位连接码;
3. 填写设备名称(下次连接自动记住);
4. 点击「连接」→ 进入 dashboard。

会话有效期:访问令牌 2 小时(自动刷新),刷新令牌 30 天。**刷新页面不会丢会话**。

### 6.2 文件浏览 / 预览 / 下载

进入 **「文件浏览」**:

- 首屏显示主机授权的**共享目录列表**,点击进入逐级浏览(面包屑可回跳);
- 点击可预览文件 → 弹出**预览模态框**(上限 10MB)；**关闭方式：点击右上角「关闭」按钮或按 ESC**——单击模态框背景不会关闭，可放心在预览内操作：
  - **图片**：顶部工具栏含 **−/+（缩小/放大）**、**适应窗口（F）**、**原始大小（0）**、**左/右旋转（R）**；同时支持**滚轮缩放**、**鼠标拖拽平移**、**双指捏合缩放**（触控屏）和**双击切换 1×/2×**；右上角显示原始像素尺寸；
  - **PDF**：调用浏览器内置 PDF 阅读器（无需插件），自带**缩放、翻页、搜索**等完整控件，直接在阅读器内操作即可；
  - **文本 / 代码**：内联渲染，可直接阅读；不可预览的格式会提示并给出直接下载入口；
- 点击不可预览文件直接触发下载；或在预览框内点击**「下载」** → 右侧「下载管理」面板显示实时进度；
- 下载走 Relay 中继隧道（二进制帧分块传输），支持**断点续传**（HTTP Range）。

被拒绝的访问(目录不在白名单、路径穿越等)会明确报错,并记入主机端安全日志。

### 6.3 消息 / 文件发送 / 安全审计 / 设置

- **消息**:与主机互发文字消息(双端持久化,重新连接后历史记录仍可查看);
- **向桌面端发送文件**:消息输入框左侧的 **📎 按钮** 可选择本地文件发送至桌面端。
  - 支持格式：图片、视频、Word/Excel/PPT、PDF、ZIP/RAR/7z 等压缩包、Markdown；
  - 发送进度以百分比实时显示在消息气泡中；完成后显示「**已送达桌面端**」及文件保存名；失败时显示「发送失败」；
  - 可一次选择多个文件，逐个顺序发送；
- **安全审计**:查看本会话相关的安全事件;
- **设置**:
  - 设备标签:保存后下次连接生效(连接页自动填入);
  - 主题(深色 / 浅色):点击即切换,刷新后保持;
  - 连接信息:查看会话 ID、主机名、系统;
  - 断开连接:断开并返回连接页(侧边栏底部的「断开连接」同理)。

### 6.4 会话被吊销时

主机端吊销你的会话后,页面会立即跳回连接页并提示「会话已被主机吊销」,需要主机重新生成 PIN 才能再次连接。

---
## 七、核心安全机制与高级特性

本节面向开发者与部署者,梳理 v1.3 引入的安全机制与协议细节。

### 7.1 WebSocket 连接参数(?type=host / ?type=client)

建立 WebSocket 连接时**必须**携带 `type` 查询参数,Relay 据此区分流量角色并施加对应的认证与限流策略。缺失或非法值将以 code `4001` 关闭连接。

| 角色 | URL 示例 | 认证方式 |
|---|---|---|
| 桌面端 Host | `ws://<relay>/ws?type=host&token=<Host JWT>` | Host JWT(长期令牌) |
| Web 客户端 | `ws://<relay>/ws?type=client&ticket=<一次性票据>` | 30 秒单次票据(ticket) |

Web 端采用「票据」模式:浏览器无法在 WS URL 里安全传递 httpOnly Cookie,因此先通过 `GET /auth/ws-ticket` 用 Cookie 换取一个 30 秒单次使用的随机票据,再用 `?ticket=` 建立 WebSocket。Host 端继续使用 `?token=` 直连,无需票据。两条路径都受 `@fastify/rate-limit` 约束。

### 7.2 Host JWT 自动轮换

Host 端签发的长期令牌有效期 **90 天**(`HOST_TOKEN_EXPIRY`)。桌面端内置 `token-rotator`,在令牌剩余有效期 ≤ 30 天时自动调用 `POST /auth/host-token-refresh` 换新 token,无需人工重新注册主机。

该端点受签名校验 + DB 封禁检查 + 限流(5 次/分钟/Host)三重保护;不在 DB 中的主机仅靠 JWT 签名校验即放行(无记录可查封禁状态)。

### 7.3 httpOnly Cookie(02a-S11)

Web 客户端的 access/refresh token **不再出现在响应 body 或 JS 可读位置**,改为写入浏览器 Cookie:

```
Set-Cookie: rb_access=...;  HttpOnly; SameSite=Strict; Path=/; Max-Age=7200   # access, 2 小时
Set-Cookie: rb_refresh=...; HttpOnly; SameSite=Strict; Path=/; Max-Age=2592000  # refresh, 30 天
```

- `HttpOnly` 使 JS 完全无法读取,任何 XSS 都无法直接窃取 token(攻击者只能盲发带 `credentials:'include'` 的请求,但响应里也不再回显明文 token)。
- 生产环境(`NODE_ENV=production`)自动追加 `Secure` 标志,仅通过 HTTPS 传输。
- 登出(`POST /auth/logout`)会置空两个 Cookie。

Relay 路由通过统一的 `extractTokenFromRequest()` 同时支持 Authorization 头(Host/旧客户端)与 Cookie(Web 端)两条路径。

### 7.4 限流(Rate Limit)

通过 `@fastify/rate-limit` 实现,采用 `global: false` 策略 —— 仅对显式声明 `config.rateLimit` 的路由生效,避免误伤健康检查等内部端点。超限返回 `429`。

| 路由 | 限制 |
|---|---|
| `POST /auth/register-host` | 按配置 |
| `POST /auth/generate-pin` | 5 次/分钟/Host |
| `POST /auth/connect` | 按 AUTH_MAX 档 |
| `POST /auth/refresh` | 按 AUTH_MAX 档(唯一未限流的认证端点已在 v1.3 补上) |
| `GET  /auth/ws-ticket` | 20 次/分钟 |
| `POST /auth/host-token-refresh` | 5 次/分钟/Host |
| `GET  /api/v1/proxy/*`(文件隧道) | 10 次/分钟 |

### 7.5 90 天数据保留

`security_logs` 与 `messages` 两张表会无限增长,拖慢全表扫描并阻塞单事件循环。v1.3 引入每日定时清理任务(`startRetentionJob`),保留 **90 天**数据。清理以批处理执行(每批最多删除 500 行),防止单次大量 `DELETE` 阻塞事件循环。Host 身份、会话等核心记录不受影响。

### 7.6 二进制文件隧道

文件下载/预览走 Relay 中继隧道,服务器不落盘。传输基于 **WebSocket 二进制帧分块**,支持:

- **断点续传**:HTTP Range 请求 → 服务器从指定偏移读取 → 续传无需重头下载;
- **流式进度**:`download-manager` 在流式下载时回写真实进度/速度/ETA 到 UI;
- **10MB 预览上限**:超过的直接走下载,避免大文件撑爆预览模态框。

### 7.7 自动更新(electron-updater)

打包安装版(非开发模式)启动约 10 秒后静默检查 GitHub Releases。状态机:待检查 → 发现新版本(可下载) → 下载中(百分比) → 已下载(可安装) → 检查失败(可关闭)。无法访问 GitHub 时忽略横幅,不强制更新,关闭后本次运行不再提示。详见桌面端操作流程 §5.6。

### 7.8 CORS 验证函数(validateOrigin)

CORS 策略唯一定义于 `apps/server/src/utils/cors.ts`,两个消费方共用同一份规则:

| 来源 | 处理 |
|---|---|
| 白名单(`ALLOWED_ORIGINS`,默认 `http://localhost:3000`、`http://localhost:9666`) | 反射 Origin + 允许凭据 |
| 任意 `localhost:`* / `127.0.0.1:`* | 放行(开发友好) |
| 其它来源 | 拒绝(响应不带 ACAO,浏览器拦截) |
| 无 Origin 头(非浏览器客户端) | 放行 |

该策略同时覆盖两处:
1. `@fastify/cors` 插件 —— 所有常规路由;
2. `routes/proxy.ts` 的隧道响应 —— `reply.hijack()` 之后插件 `onSend` 不再执行,必须用 `corsHeadersFor()` 手动补头,否则浏览器会拦截代理的下载/预览。

> 修改 CORS 策略只改 `cors.ts` 一处即可,两侧自动保持一致。


## 八、验证与测试(开发者)

```sh
# 全套自动化测试（无需手动启动 relay，globalSetup 自动在 3099 端口起/停测试用 relay）
pnpm --filter @remotebridge/server test    # server：继电器路由、会话流、文件隧道、CORS、限流等（110+ 用例）
pnpm --filter @remotebridge/desktop test   # desktop：路径守卫、Token 管理、文件隧道、审计（64 用例）
pnpm --filter @remotebridge/web test       # web：WebSocket 重连/退避状态机（16 用例）
pnpm --filter @remotebridge/shared test    # shared：文件工具、安全校验、二进制帧编解码（42 用例）

# 可选：在已运行的 relay（3099）上跑 server 套件（globalSetup 会自动复用）
$env:RELAY_PORT=3099; pnpm --filter @remotebridge/server dev   # 终端 1
pnpm --filter @remotebridge/server test                         # 终端 2
```

`apps/server/test/manual-*.mjs` 为针对真实环境的验证脚本(非 vitest):

| 脚本 | 用途 |
|---|---|
| `manual-live-host.mjs` | 对真实桌面端的 12 项全链路断言(认证/目录/下载隧道/Range/安全拦截) |
| `manual-trust-revoke.mjs` | 信任/吊销功能(CDP 驱动桌面 UI,需 electron 带 `--remote-debugging-port=9222` 启动) |
| `manual-settings-hot-reload.mjs` | 桌面端设置热重载(Relay 切换 + 主题) |
| `manual-file-tunnel.mjs` 等 | 文件隧道 / 重连 / 消息持久化专项 |

---

## 九、常见问题排查

| 现象 | 原因与处理 |
|---|---|
| Relay 启动报 `NODE_MODULE_VERSION ... requires ...` | better-sqlite3 二进制被 Electron 版覆盖 → `pnpm --filter @remotebridge/server rebuild better-sqlite3` |
| 桌面端显示"连接失败 / EACCES ::1:3002" | Relay 地址用了 `localhost` → 改为 `127.0.0.1`(设置页或配置文件) |
| 端口被占用(9666/3002) | `netstat -ano \| findstr :3002` 找到 PID 后结束进程,或换 `RELAY_PORT` |
| Web 输入 PIN 提示无效 | PIN 已过期(默认 5 分钟)或已被使用 → 主机端重新生成 |
| Web 文件页一直转圈 | 主机离线(看侧边栏状态);或目录刚被移出白名单 |
| 浏览器下载/预览失败且控制台有 CORS 错误 | Relay 的 `ALLOWED_ORIGINS` 未包含 Web 站点来源 → 补环境变量后重启 Relay |
| 桌面端窗口白屏 | 点击界面上的「重新加载」按钮(已内置错误兜底页) |
| 修改了 `packages/shared` 后行为异常 | 忘记重建 → `pnpm --filter @remotebridge/shared build` 后重启相关端 |
| 消息中心发送文件一直卡在进度、不完成 | Relay 服务器版本低于 v1.1.1（旧版不路由文件传输消息）→ 将 Relay 更新至 v1.1.1 并重启 |
| 安全审计日志显示"无法连接到中继服务器" | Relay 未运行或地址配置错误 → 先启动 Relay（见 4.1），再点击日志页面内「重试」按钮 |
| 桌面端顶部显示更新错误横幅 | 无法访问 GitHub Releases（网络限制）→ 点击横幅右侧 **×** 关闭即可，不影响正常功能 |
| PDF 预览空白 / 无法显示 | 浏览器禁用了 PDF 插件或使用了不支持内联 PDF 的环境 → 点「下载」后用本地 PDF 阅读器打开 |

**数据存储位置**(备份/重置时参考):

| 数据 | 位置 |
|---|---|
| Relay 数据库(主机/会话/安全日志) | `~/.remotebridge/data/remotebridge.db`(可用 `RB_DATA_DIR` 改) |
| 桌面端配置(身份/Relay 地址/主题) | `AppData\Roaming\@remotebridge\desktop\remotebridge-config.json` |
| 桌面端本地库(白名单/客户端/消息/日志) | `AppData\Roaming\@remotebridge\desktop\data\local.db` |
| Web 端令牌(access/refresh) | 浏览器 httpOnly Cookie（清除站点 Cookie 即重置，JS 不可读，防 XSS） |

---

## 十、快速上手清单(TL;DR)

```sh
# —— 终端 1:Relay（生产模式，Git Bash）——
bash scripts/deploy-server.sh          # 构建 shared + server
cd apps/server && node dist/index.js   # 启动（读取 .env，NODE_ENV=production）

# —— 终端 2:Web ——
pnpm --filter @remotebridge/web dev

# —— 终端 3:桌面端 ——
pnpm --filter @remotebridge/desktop build
cd apps/desktop && .\node_modules\.bin\electron .
```

1. 桌面端左下角变绿(已连接);
2. 桌面端「共享目录」添加一个文件夹(选"可下载");
3. 桌面端「连接状态」生成连接码;
4. 浏览器打开 `http://localhost:9666`,输入连接码连接;
5. 「文件浏览」里点开目录 → 预览 / 下载文件。
